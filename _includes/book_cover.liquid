<div class="grid-item">
    <a href="{{ book.url | relative_url }}">
        <img 
            src="{{ book.img | relative_url }}"
            loading="lazy"
            width="100%"
        />
    </a>
</div>